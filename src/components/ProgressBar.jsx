export default function ProgressBar({ value, max, onChange, compact = false }) { const safeMax = max || 1; const safeValue = Math.min(value || 0, safeMax); const percent = `${(safeValue / safeMax) * 100}%`; return <input aria-label={compact ? 'Volume' : 'Seek'} className={`progress ${compact ? 'compact' : ''}`} style={{ '--progress-percent': percent }} type="range" min="0" max={safeMax} step="0.1" value={safeValue} onChange={(e) => onChange(Number(e.target.value))} /> }
