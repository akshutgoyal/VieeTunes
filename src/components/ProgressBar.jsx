export default function ProgressBar({ value, max, onChange, compact = false }) { const safeMax = max || 1; const safeValue = Math.min(value || 0, safeMax); const percent = `${(safeValue / safeMax) * 100}%`; return <input aria-label="Seek" className={`progress ${compact ? 'compact' : ''}`} style={{ background: `linear-gradient(to right, #0dcfcf 0%, #0dcfcf ${percent}, #585858 ${percent}, #585858 100%)` }} type="range" min="0" max={safeMax} step="0.1" value={safeValue} onChange={(e) => onChange(Number(e.target.value))} /> }
