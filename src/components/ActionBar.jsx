import { usePlayerStore } from '../store/playerStore'
export default function ActionBar() { const s = usePlayerStore(); const toggle = () => s.isPlaying ? s.togglePlay() : s.currentTrack ? s.togglePlay() : s.startPlaylist(); return <div className="action-bar"><button className="big-play" aria-label={s.isPlaying ? 'Pause playlist' : 'Play playlist'} onClick={toggle}>{s.isPlaying ? <span className="pause-icon"/> : <span className="play-icon"/>}</button><button className={s.isShuffle ? 'action selected' : 'action'} aria-label="Shuffle playlist" onClick={s.startShuffle}>⤨</button><button className="action" aria-label="Download playlist">⇩</button><button className="action" aria-label="Add collaborator">♧</button><button className="action" aria-label="More options">•••</button><div className="view-mode">List&nbsp; ☷</div></div> }
