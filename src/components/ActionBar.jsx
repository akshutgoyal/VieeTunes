import { usePlayerStore } from '../store/playerStore'
export default function ActionBar() { const s = usePlayerStore(); return <div className="action-bar"><button className="big-play" aria-label="Play playlist from the beginning" onClick={s.startPlaylist}>{s.isPlaying ? <span className="pause-icon"/> : <span className="play-icon"/>}</button><button className={s.isShuffle ? 'action selected' : 'action'} aria-label="Shuffle playlist" onClick={s.startShuffle}>⤨</button><button className="action">⇩</button><button className="action">♧</button><button className="action">•••</button><div className="view-mode">List&nbsp; ☷</div></div> }
