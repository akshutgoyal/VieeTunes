import { usePlayerStore } from '../store/playerStore'
import Icon from './Icons'
export default function ActionBar() { const s = usePlayerStore(); const toggle = () => s.isPlaying ? s.togglePlay() : s.currentTrack ? s.togglePlay() : s.startPlaylist(); return <div className="action-bar"><button className="big-play" aria-label={s.isPlaying ? 'Pause playlist' : 'Play playlist'} onClick={toggle}>{s.isPlaying ? <span className="pause-icon"/> : <span className="play-icon"/>}</button><button className={s.isShuffle ? 'action selected' : 'action'} aria-label="Shuffle playlist" onClick={s.startShuffle}><Icon name="shuffle"/></button><button className="action" aria-label="Download playlist"><Icon name="download"/></button><button className="action" aria-label="Add collaborator"><Icon name="users"/></button><button className="action" aria-label="More options"><Icon name="more"/></button><div className="view-mode">List&nbsp; ☷</div></div> }
