import { usePlayerStore } from '../store/playerStore'
import ProgressBar from './ProgressBar'
const fmt = (n) => `${Math.floor(n / 60)}:${String(Math.floor(n % 60)).padStart(2, '0')}`
export default function PlayerBar() { const s = usePlayerStore(); if (!s.currentTrack) return null; return <footer className="player"><div className="now-playing"><img src={s.currentTrack.cover} alt=""/><div><strong>{s.currentTrack.title}</strong><span>{s.currentTrack.artist}</span></div><button className="like">♡</button></div><div className="controls"><div className="control-buttons"><button className={s.isShuffle ? 'selected' : ''} onClick={s.toggleShuffle}>⤨</button><button onClick={s.previous}>◀◀</button><button className="play-pause" aria-label={s.isPlaying ? 'Pause' : 'Play'} onClick={s.togglePlay}>{s.isPlaying ? <span className="pause-icon"/> : <span className="play-icon"/>}</button><button onClick={s.next}>▶▶</button><button className={s.repeat !== 'off' ? 'selected' : ''} onClick={s.toggleRepeat}>↻</button></div><div className="timeline"><span>{fmt(s.currentTime)}</span><ProgressBar value={s.currentTime} max={s.duration || s.currentTrack.duration} onChange={s.seek}/><span>{fmt(s.duration || s.currentTrack.duration)}</span></div></div><div className="player-tools"><button>♧</button><button>☷</button><button onClick={() => s.setVolume(s.volume ? 0 : .75)}>◖</button><ProgressBar compact value={s.volume} max={1} onChange={s.setVolume}/><button>▣</button><button>⛶</button></div></footer> }
